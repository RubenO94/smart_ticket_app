import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:smart_ticket/models/device.dart';

class DeviceProvider extends StateNotifier<Device> {
  DeviceProvider(super.state);

  
  
}