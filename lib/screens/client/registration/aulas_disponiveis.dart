import 'package:flutter/widgets.dart';

class AulasDisponiveisScreen extends StatelessWidget {
  const AulasDisponiveisScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
